{
  "name": "sandbox",
  "private": true,
  "devDependencies": {
    "git-hooks": "^1.1.10",
    "lerna": "^3.4.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "cov": "lerna run cov"
  }
}
