{
  "name": "sandbox",
  "private": true,
  "devDependencies": {
    "nyc": "^11.2.1",
    "git-hooks": "^1.1.10",
    "lerna": "^3.4.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "cov": "sh scripts/cov.sh"
  }
}
